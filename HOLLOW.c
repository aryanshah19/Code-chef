#include <stdio.h>
int main()
{
  int N,C;
  scanf("%d%d",&N,&C);
  int arr[N][C];
    for (int i = 0; i < N; ++i)
    {
        for (int j = 0; j < C; ++j)
        {
          scanf("%d", &arr[N][C]);
        }
    }
  
}
