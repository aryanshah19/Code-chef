#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        int h,m,temp;
        char arr;
        scanf("%d:%d %s",&h,&m,&arr);
        int p;
        scanf("%d",&p);
        for(int i=0;i<p;i++)
            {
                int h1,m1,temp1,h2,m2;
                char arr1,arr2;
                scanf("%d:%d %s %d:%d %s",&h1,&m1,&arr1,&h2,&m2,&arr2);
                if(arr=='A')
                {
                    if(arr1=='A' && arr1=='P')
                        
                }
                
                
            }
    }
}
